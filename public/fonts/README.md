# Brand Fonts

Place the provided font files here when they are available.

Recommended filenames:

- `Khand.woff2`
- `Switzer.woff2`

The CSS already declares `Khand` for headings and `Switzer` for body copy with sensible fallbacks, so the site remains usable before the licensed font files are added.
